export const DEFAULT_CHANNELS = ['general', 'random', 'help'];
export const MESSAGE_LIMIT = 50;
export const WS_RECONNECT_BASE = 1000;
export const WS_RECONNECT_MAX = 30000;
export const TOKEN_REFRESH_BUFFER_MS = 5 * 60 * 1000; // refresh 5 min before expiry
